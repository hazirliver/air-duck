[project]
name = "ducklake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.3.1",
    "marimo>=0.14.9",
]

[dependency-groups]
dev = [
    "ruff>=0.12.1",
]
